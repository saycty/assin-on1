name: PR checks — Lint and Test

on:
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js 24.2.0
        uses: actions/setup-node@v4
        with:
          node-version: "24.2.0"

      - name: Install dependencies (use legacy peer deps)
        run: npm install --legacy-peer-deps --no-audit --no-fund

      - name: Run linter
        run: npm run lint

      - name: Run tests
        run: npm run test --silent
